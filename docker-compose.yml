services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: acm
      POSTGRES_PASSWORD: acm_dev_password
      POSTGRES_DB: acm
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: